# =============================================================
# 墨海寻珠 · Python 依赖（pip 唯一口径）
#
# 本地（conda 环境 naimore3-docs 内）：
#   conda run -n naimore3-docs pip install -r requirements.txt
#
# 远端 / CI / Cloudflare Pages：
#   pip install -r requirements.txt && mkdocs build
#
# 说明：
# - 版本用 == 锁定为本地已验证的组合（与原 environment.yml 的 pip 段一致）。
# - python=3.10 与 nodejs 不在本文件里：conda 环境用
#     conda create -n naimore3-docs -c conda-forge python=3.10 pip
#     conda install -n naimore3-docs -c conda-forge nodejs=22
#   nodejs 仅本地拉取前端库用，站点构建不依赖 Node。
# =============================================================

# MkDocs 核心与 Material 主题
mkdocs==1.6.1
mkdocs-material==9.7.7

# 中文搜索分词（装好后 Material 的搜索会自动使用 jieba）
jieba==0.42.1

# 常用插件（均已安装；是否启用看 mkdocs.yml 的 plugins:）
mkdocs-minify-plugin==0.8.0
mkdocs-redirects==1.2.3
mkdocs-git-revision-date-localized-plugin==1.6.0

# Markdown 扩展
pymdown-extensions==11.0.2
